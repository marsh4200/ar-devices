substitutions:
  device_model: Sonoff Switch

script:
  - id: publish_button_event
    mode: queued
    parameters:
      button: int
      action: string
      count: int
    then:
      - homeassistant.event:
          event: esphome.sonoff_switch_button
          data:
            model: ${device_model}
            action: !lambda return action.c_str();
            button: !lambda return button;
            count: !lambda return count;
      - if:
          condition:
            lambda: return button == 1;
          then:
            - text_sensor.template.publish:
                id: ts_button_1_last_action
                state: !lambda return action.c_str();
      - if:
          condition:
            lambda: return button == 2;
          then:
            - text_sensor.template.publish:
                id: ts_button_2_last_action
                state: !lambda return action.c_str();
      - if:
          condition:
            lambda: return button == 3;
          then:
            - text_sensor.template.publish:
                id: ts_button_3_last_action
                state: !lambda return action.c_str();
      - text_sensor.template.publish:
          id: ts_last_button_event
          state: !lambda |-
            char buffer[48];
            snprintf(buffer, sizeof(buffer), "Button %d: %s (%d)", button, action.c_str(), count);
            return std::string(buffer);

  - id: run_relay_momentary
    mode: parallel
    parameters:
      relay: int
      duration_ms: int
    then:
      - if:
          condition:
            lambda: return relay == 1;
          then:
            - switch.turn_on: relay_1
      - if:
          condition:
            lambda: return relay == 2;
          then:
            - switch.turn_on: relay_2
      - if:
          condition:
            lambda: return relay == 3;
          then:
            - switch.turn_on: relay_3
      - delay: !lambda return (uint32_t) duration_ms;
      - if:
          condition:
            lambda: return relay == 1;
          then:
            - switch.turn_off: relay_1
      - if:
          condition:
            lambda: return relay == 2;
          then:
            - switch.turn_off: relay_2
      - if:
          condition:
            lambda: return relay == 3;
          then:
            - switch.turn_off: relay_3

  - id: run_homeassistant_momentary
    mode: parallel
    parameters:
      entity_id: string
      duration_ms: int
    then:
      - homeassistant.action:
          action: homeassistant.turn_on
          data:
            entity_id: !lambda return entity_id.c_str();
      - delay: !lambda return (uint32_t) duration_ms;
      - homeassistant.action:
          action: homeassistant.turn_off
          data:
            entity_id: !lambda return entity_id.c_str();

  - id: handle_single_click
    mode: queued
    parameters:
      button: int
    then:
      - script.execute:
          id: publish_button_event
          button: !lambda return button;
          action: "click"
          count: 1
      - if:
          condition:
            lambda: return button == 1;
          then:
            - if:
                condition:
                  switch.is_on: sw_button_1_click_momentary_enabled
                then:
                  - script.execute:
                      id: run_relay_momentary
                      relay: 1
                      duration_ms: !lambda return (int) (id(num_button_1_click_momentary_s).state * 1000.0f);
                else:
                  - switch.toggle: relay_1
      - if:
          condition:
            lambda: return button == 2;
          then:
            - if:
                condition:
                  switch.is_on: sw_button_2_click_momentary_enabled
                then:
                  - script.execute:
                      id: run_relay_momentary
                      relay: 2
                      duration_ms: !lambda return (int) (id(num_button_2_click_momentary_s).state * 1000.0f);
                else:
                  - switch.toggle: relay_2
      - if:
          condition:
            lambda: return button == 3;
          then:
            - if:
                condition:
                  switch.is_on: sw_button_3_click_momentary_enabled
                then:
                  - script.execute:
                      id: run_relay_momentary
                      relay: 3
                      duration_ms: !lambda return (int) (id(num_button_3_click_momentary_s).state * 1000.0f);
                else:
                  - switch.toggle: relay_3

  - id: handle_long_press
    mode: queued
    parameters:
      button: int
    then:
      - script.execute:
          id: publish_button_event
          button: !lambda return button;
          action: "long_press"
          count: 1
      - lambda: |-
          std::string configured_action = "None";
          std::string target_entity_id;
          if (button == 1) {
            configured_action = id(sl_button_1_long_press_action).state;
            target_entity_id = id(txt_button_1_long_press_target).state;
          } else if (button == 2) {
            configured_action = id(sl_button_2_long_press_action).state;
            target_entity_id = id(txt_button_2_long_press_target).state;
          } else if (button == 3) {
            configured_action = id(sl_button_3_long_press_action).state;
            target_entity_id = id(txt_button_3_long_press_target).state;
          }
          ESP_LOGI("long_press",
                   "Button %d long-press action: '%s' target='%s'",
                   button,
                   configured_action.c_str(),
                   target_entity_id.c_str());
      - if:
          condition:
            lambda: |-
              if (button == 1) return id(sl_button_1_long_press_action).state == "Relay 1 (toggle)";
              if (button == 2) return id(sl_button_2_long_press_action).state == "Relay 1 (toggle)";
              return id(sl_button_3_long_press_action).state == "Relay 1 (toggle)";
          then:
            - switch.toggle: relay_1
      - if:
          condition:
            lambda: |-
              if (button == 1) return id(sl_button_1_long_press_action).state == "Relay 1 (momentary)";
              if (button == 2) return id(sl_button_2_long_press_action).state == "Relay 1 (momentary)";
              return id(sl_button_3_long_press_action).state == "Relay 1 (momentary)";
          then:
            - script.execute:
                id: run_relay_momentary
                relay: 1
                duration_ms: !lambda |-
                  if (button == 1) return (int) (id(num_button_1_long_press_momentary_s).state * 1000.0f);
                  if (button == 2) return (int) (id(num_button_2_long_press_momentary_s).state * 1000.0f);
                  return (int) (id(num_button_3_long_press_momentary_s).state * 1000.0f);
      - if:
          condition:
            lambda: |-
              if (button == 1) return id(sl_button_1_long_press_action).state == "Relay 2 (toggle)";
              if (button == 2) return id(sl_button_2_long_press_action).state == "Relay 2 (toggle)";
              return id(sl_button_3_long_press_action).state == "Relay 2 (toggle)";
          then:
            - switch.toggle: relay_2
      - if:
          condition:
            lambda: |-
              if (button == 1) return id(sl_button_1_long_press_action).state == "Relay 2 (momentary)";
              if (button == 2) return id(sl_button_2_long_press_action).state == "Relay 2 (momentary)";
              return id(sl_button_3_long_press_action).state == "Relay 2 (momentary)";
          then:
            - script.execute:
                id: run_relay_momentary
                relay: 2
                duration_ms: !lambda |-
                  if (button == 1) return (int) (id(num_button_1_long_press_momentary_s).state * 1000.0f);
                  if (button == 2) return (int) (id(num_button_2_long_press_momentary_s).state * 1000.0f);
                  return (int) (id(num_button_3_long_press_momentary_s).state * 1000.0f);
      - if:
          condition:
            lambda: |-
              if (button == 1) return id(sl_button_1_long_press_action).state == "Relay 3 (toggle)";
              if (button == 2) return id(sl_button_2_long_press_action).state == "Relay 3 (toggle)";
              return id(sl_button_3_long_press_action).state == "Relay 3 (toggle)";
          then:
            - switch.toggle: relay_3
      - if:
          condition:
            lambda: |-
              if (button == 1) return id(sl_button_1_long_press_action).state == "Relay 3 (momentary)";
              if (button == 2) return id(sl_button_2_long_press_action).state == "Relay 3 (momentary)";
              return id(sl_button_3_long_press_action).state == "Relay 3 (momentary)";
          then:
            - script.execute:
                id: run_relay_momentary
                relay: 3
                duration_ms: !lambda |-
                  if (button == 1) return (int) (id(num_button_1_long_press_momentary_s).state * 1000.0f);
                  if (button == 2) return (int) (id(num_button_2_long_press_momentary_s).state * 1000.0f);
                  return (int) (id(num_button_3_long_press_momentary_s).state * 1000.0f);
      - if:
          condition:
            lambda: |-
              if (button == 1) return id(sl_button_1_long_press_action).state == "Home Assistant target (toggle)" && !id(txt_button_1_long_press_target).state.empty();
              if (button == 2) return id(sl_button_2_long_press_action).state == "Home Assistant target (toggle)" && !id(txt_button_2_long_press_target).state.empty();
              return id(sl_button_3_long_press_action).state == "Home Assistant target (toggle)" && !id(txt_button_3_long_press_target).state.empty();
          then:
            - homeassistant.action:
                action: homeassistant.toggle
                data:
                  entity_id: !lambda |-
                    if (button == 1) return id(txt_button_1_long_press_target).state.c_str();
                    if (button == 2) return id(txt_button_2_long_press_target).state.c_str();
                    return id(txt_button_3_long_press_target).state.c_str();
      - if:
          condition:
            lambda: |-
              if (button == 1) return id(sl_button_1_long_press_action).state == "Home Assistant target (turn on)" && !id(txt_button_1_long_press_target).state.empty();
              if (button == 2) return id(sl_button_2_long_press_action).state == "Home Assistant target (turn on)" && !id(txt_button_2_long_press_target).state.empty();
              return id(sl_button_3_long_press_action).state == "Home Assistant target (turn on)" && !id(txt_button_3_long_press_target).state.empty();
          then:
            - homeassistant.action:
                action: homeassistant.turn_on
                data:
                  entity_id: !lambda |-
                    if (button == 1) return id(txt_button_1_long_press_target).state.c_str();
                    if (button == 2) return id(txt_button_2_long_press_target).state.c_str();
                    return id(txt_button_3_long_press_target).state.c_str();
      - if:
          condition:
            lambda: |-
              if (button == 1) return id(sl_button_1_long_press_action).state == "Home Assistant target (turn off)" && !id(txt_button_1_long_press_target).state.empty();
              if (button == 2) return id(sl_button_2_long_press_action).state == "Home Assistant target (turn off)" && !id(txt_button_2_long_press_target).state.empty();
              return id(sl_button_3_long_press_action).state == "Home Assistant target (turn off)" && !id(txt_button_3_long_press_target).state.empty();
          then:
            - homeassistant.action:
                action: homeassistant.turn_off
                data:
                  entity_id: !lambda |-
                    if (button == 1) return id(txt_button_1_long_press_target).state.c_str();
                    if (button == 2) return id(txt_button_2_long_press_target).state.c_str();
                    return id(txt_button_3_long_press_target).state.c_str();
      - if:
          condition:
            lambda: |-
              if (button == 1) return id(sl_button_1_long_press_action).state == "Home Assistant target (momentary)" && !id(txt_button_1_long_press_target).state.empty();
              if (button == 2) return id(sl_button_2_long_press_action).state == "Home Assistant target (momentary)" && !id(txt_button_2_long_press_target).state.empty();
              return id(sl_button_3_long_press_action).state == "Home Assistant target (momentary)" && !id(txt_button_3_long_press_target).state.empty();
          then:
            - script.execute:
                id: run_homeassistant_momentary
                entity_id: !lambda |-
                  if (button == 1) return id(txt_button_1_long_press_target).state;
                  if (button == 2) return id(txt_button_2_long_press_target).state;
                  return id(txt_button_3_long_press_target).state;
                duration_ms: !lambda |-
                  if (button == 1) return (int) (id(num_button_1_long_press_momentary_s).state * 1000.0f);
                  if (button == 2) return (int) (id(num_button_2_long_press_momentary_s).state * 1000.0f);
                  return (int) (id(num_button_3_long_press_momentary_s).state * 1000.0f);
      - if:
          condition:
            lambda: |-
              if (button == 1) return id(sl_button_1_long_press_action).state == "Cover target (open)" && !id(txt_button_1_long_press_target).state.empty();
              if (button == 2) return id(sl_button_2_long_press_action).state == "Cover target (open)" && !id(txt_button_2_long_press_target).state.empty();
              return id(sl_button_3_long_press_action).state == "Cover target (open)" && !id(txt_button_3_long_press_target).state.empty();
          then:
            - homeassistant.action:
                action: cover.open_cover
                data:
                  entity_id: !lambda |-
                    if (button == 1) return id(txt_button_1_long_press_target).state.c_str();
                    if (button == 2) return id(txt_button_2_long_press_target).state.c_str();
                    return id(txt_button_3_long_press_target).state.c_str();
      - if:
          condition:
            lambda: |-
              if (button == 1) return id(sl_button_1_long_press_action).state == "Cover target (close)" && !id(txt_button_1_long_press_target).state.empty();
              if (button == 2) return id(sl_button_2_long_press_action).state == "Cover target (close)" && !id(txt_button_2_long_press_target).state.empty();
              return id(sl_button_3_long_press_action).state == "Cover target (close)" && !id(txt_button_3_long_press_target).state.empty();
          then:
            - homeassistant.action:
                action: cover.close_cover
                data:
                  entity_id: !lambda |-
                    if (button == 1) return id(txt_button_1_long_press_target).state.c_str();
                    if (button == 2) return id(txt_button_2_long_press_target).state.c_str();
                    return id(txt_button_3_long_press_target).state.c_str();
      - if:
          condition:
            lambda: |-
              if (button == 1) return id(sl_button_1_long_press_action).state == "Cover target (stop)" && !id(txt_button_1_long_press_target).state.empty();
              if (button == 2) return id(sl_button_2_long_press_action).state == "Cover target (stop)" && !id(txt_button_2_long_press_target).state.empty();
              return id(sl_button_3_long_press_action).state == "Cover target (stop)" && !id(txt_button_3_long_press_target).state.empty();
          then:
            - homeassistant.action:
                action: cover.stop_cover
                data:
                  entity_id: !lambda |-
                    if (button == 1) return id(txt_button_1_long_press_target).state.c_str();
                    if (button == 2) return id(txt_button_2_long_press_target).state.c_str();
                    return id(txt_button_3_long_press_target).state.c_str();
      - if:
          condition:
            lambda: |-
              if (button == 1) return id(sl_button_1_long_press_action).state == "Cover target (toggle)" && !id(txt_button_1_long_press_target).state.empty();
              if (button == 2) return id(sl_button_2_long_press_action).state == "Cover target (toggle)" && !id(txt_button_2_long_press_target).state.empty();
              return id(sl_button_3_long_press_action).state == "Cover target (toggle)" && !id(txt_button_3_long_press_target).state.empty();
          then:
            - homeassistant.action:
                action: homeassistant.toggle
                data:
                  entity_id: !lambda |-
                    if (button == 1) return id(txt_button_1_long_press_target).state.c_str();
                    if (button == 2) return id(txt_button_2_long_press_target).state.c_str();
                    return id(txt_button_3_long_press_target).state.c_str();
      - if:
          condition:
            lambda: |-
              if (button == 1) return id(sl_button_1_long_press_action).state == "Media player target (play/pause)" && !id(txt_button_1_long_press_target).state.empty();
              if (button == 2) return id(sl_button_2_long_press_action).state == "Media player target (play/pause)" && !id(txt_button_2_long_press_target).state.empty();
              return id(sl_button_3_long_press_action).state == "Media player target (play/pause)" && !id(txt_button_3_long_press_target).state.empty();
          then:
            - homeassistant.action:
                action: media_player.media_play_pause
                data:
                  entity_id: !lambda |-
                    if (button == 1) return id(txt_button_1_long_press_target).state.c_str();
                    if (button == 2) return id(txt_button_2_long_press_target).state.c_str();
                    return id(txt_button_3_long_press_target).state.c_str();
      - if:
          condition:
            lambda: |-
              if (button == 1) return id(sl_button_1_long_press_action).state == "Media player target (next track)" && !id(txt_button_1_long_press_target).state.empty();
              if (button == 2) return id(sl_button_2_long_press_action).state == "Media player target (next track)" && !id(txt_button_2_long_press_target).state.empty();
              return id(sl_button_3_long_press_action).state == "Media player target (next track)" && !id(txt_button_3_long_press_target).state.empty();
          then:
            - homeassistant.action:
                action: media_player.media_next_track
                data:
                  entity_id: !lambda |-
                    if (button == 1) return id(txt_button_1_long_press_target).state.c_str();
                    if (button == 2) return id(txt_button_2_long_press_target).state.c_str();
                    return id(txt_button_3_long_press_target).state.c_str();
      - if:
          condition:
            lambda: |-
              if (button == 1) return id(sl_button_1_long_press_action).state == "Media player target (previous track)" && !id(txt_button_1_long_press_target).state.empty();
              if (button == 2) return id(sl_button_2_long_press_action).state == "Media player target (previous track)" && !id(txt_button_2_long_press_target).state.empty();
              return id(sl_button_3_long_press_action).state == "Media player target (previous track)" && !id(txt_button_3_long_press_target).state.empty();
          then:
            - homeassistant.action:
                action: media_player.media_previous_track
                data:
                  entity_id: !lambda |-
                    if (button == 1) return id(txt_button_1_long_press_target).state.c_str();
                    if (button == 2) return id(txt_button_2_long_press_target).state.c_str();
                    return id(txt_button_3_long_press_target).state.c_str();
      - if:
          condition:
            lambda: |-
              if (button == 1) return id(sl_button_1_long_press_action).state == "Media player target (stop)" && !id(txt_button_1_long_press_target).state.empty();
              if (button == 2) return id(sl_button_2_long_press_action).state == "Media player target (stop)" && !id(txt_button_2_long_press_target).state.empty();
              return id(sl_button_3_long_press_action).state == "Media player target (stop)" && !id(txt_button_3_long_press_target).state.empty();
          then:
            - homeassistant.action:
                action: media_player.media_stop
                data:
                  entity_id: !lambda |-
                    if (button == 1) return id(txt_button_1_long_press_target).state.c_str();
                    if (button == 2) return id(txt_button_2_long_press_target).state.c_str();
                    return id(txt_button_3_long_press_target).state.c_str();
      - if:
          condition:
            lambda: |-
              if (button == 1) return id(sl_button_1_long_press_action).state == "Script target (run)" && !id(txt_button_1_long_press_target).state.empty();
              if (button == 2) return id(sl_button_2_long_press_action).state == "Script target (run)" && !id(txt_button_2_long_press_target).state.empty();
              return id(sl_button_3_long_press_action).state == "Script target (run)" && !id(txt_button_3_long_press_target).state.empty();
          then:
            - homeassistant.action:
                action: script.turn_on
                data:
                  entity_id: !lambda |-
                    if (button == 1) return id(txt_button_1_long_press_target).state.c_str();
                    if (button == 2) return id(txt_button_2_long_press_target).state.c_str();
                    return id(txt_button_3_long_press_target).state.c_str();
      - if:
          condition:
            lambda: |-
              if (button == 1) return id(sl_button_1_long_press_action).state == "Scene target (activate)" && !id(txt_button_1_long_press_target).state.empty();
              if (button == 2) return id(sl_button_2_long_press_action).state == "Scene target (activate)" && !id(txt_button_2_long_press_target).state.empty();
              return id(sl_button_3_long_press_action).state == "Scene target (activate)" && !id(txt_button_3_long_press_target).state.empty();
          then:
            - homeassistant.action:
                action: scene.turn_on
                data:
                  entity_id: !lambda |-
                    if (button == 1) return id(txt_button_1_long_press_target).state.c_str();
                    if (button == 2) return id(txt_button_2_long_press_target).state.c_str();
                    return id(txt_button_3_long_press_target).state.c_str();
      - if:
          condition:
            lambda: |-
              if (button == 1) return id(sl_button_1_long_press_action).state == "Button target (press)" && !id(txt_button_1_long_press_target).state.empty();
              if (button == 2) return id(sl_button_2_long_press_action).state == "Button target (press)" && !id(txt_button_2_long_press_target).state.empty();
              return id(sl_button_3_long_press_action).state == "Button target (press)" && !id(txt_button_3_long_press_target).state.empty();
          then:
            - homeassistant.action:
                action: button.press
                data:
                  entity_id: !lambda |-
                    if (button == 1) return id(txt_button_1_long_press_target).state.c_str();
                    if (button == 2) return id(txt_button_2_long_press_target).state.c_str();
                    return id(txt_button_3_long_press_target).state.c_str();
      - if:
          condition:
            lambda: |-
              if (button == 1) return id(sl_button_1_long_press_action).state == "Automation target (trigger)" && !id(txt_button_1_long_press_target).state.empty();
              if (button == 2) return id(sl_button_2_long_press_action).state == "Automation target (trigger)" && !id(txt_button_2_long_press_target).state.empty();
              return id(sl_button_3_long_press_action).state == "Automation target (trigger)" && !id(txt_button_3_long_press_target).state.empty();
          then:
            - homeassistant.action:
                action: automation.trigger
                data:
                  entity_id: !lambda |-
                    if (button == 1) return id(txt_button_1_long_press_target).state.c_str();
                    if (button == 2) return id(txt_button_2_long_press_target).state.c_str();
                    return id(txt_button_3_long_press_target).state.c_str();

select:
  - platform: template
    id: sl_button_1_long_press_action
    name: Button 1 long-press action
    optimistic: true
    restore_value: true
    icon: mdi:gesture-tap-hold
    entity_category: config
    options:
      - None
      - Relay 1 (toggle)
      - Relay 1 (momentary)
      - Relay 2 (toggle)
      - Relay 2 (momentary)
      - Relay 3 (toggle)
      - Relay 3 (momentary)
      - Home Assistant target (toggle)
      - Home Assistant target (turn on)
      - Home Assistant target (turn off)
      - Home Assistant target (momentary)
      - Cover target (open)
      - Cover target (close)
      - Cover target (stop)
      - Cover target (toggle)
      - Media player target (play/pause)
      - Media player target (next track)
      - Media player target (previous track)
      - Media player target (stop)
      - Script target (run)
      - Scene target (activate)
      - Button target (press)
      - Automation target (trigger)
    initial_option: None

  - platform: template
    id: sl_button_2_long_press_action
    name: Button 2 long-press action
    optimistic: true
    restore_value: true
    icon: mdi:gesture-tap-hold
    entity_category: config
    options:
      - None
      - Relay 1 (toggle)
      - Relay 1 (momentary)
      - Relay 2 (toggle)
      - Relay 2 (momentary)
      - Relay 3 (toggle)
      - Relay 3 (momentary)
      - Home Assistant target (toggle)
      - Home Assistant target (turn on)
      - Home Assistant target (turn off)
      - Home Assistant target (momentary)
      - Cover target (open)
      - Cover target (close)
      - Cover target (stop)
      - Cover target (toggle)
      - Media player target (play/pause)
      - Media player target (next track)
      - Media player target (previous track)
      - Media player target (stop)
      - Script target (run)
      - Scene target (activate)
      - Button target (press)
      - Automation target (trigger)
    initial_option: None

  - platform: template
    id: sl_button_3_long_press_action
    name: Button 3 long-press action
    optimistic: true
    restore_value: true
    icon: mdi:gesture-tap-hold
    entity_category: config
    options:
      - None
      - Relay 1 (toggle)
      - Relay 1 (momentary)
      - Relay 2 (toggle)
      - Relay 2 (momentary)
      - Relay 3 (toggle)
      - Relay 3 (momentary)
      - Home Assistant target (toggle)
      - Home Assistant target (turn on)
      - Home Assistant target (turn off)
      - Home Assistant target (momentary)
      - Cover target (open)
      - Cover target (close)
      - Cover target (stop)
      - Cover target (toggle)
      - Media player target (play/pause)
      - Media player target (next track)
      - Media player target (previous track)
      - Media player target (stop)
      - Script target (run)
      - Scene target (activate)
      - Button target (press)
      - Automation target (trigger)
    initial_option: None

switch:
  - platform: template
    id: sw_button_1_click_momentary_enabled
    name: Button 1 click momentary
    optimistic: true
    restore_mode: RESTORE_DEFAULT_OFF
    entity_category: config
    icon: mdi:gesture-tap-button

  - platform: template
    id: sw_button_2_click_momentary_enabled
    name: Button 2 click momentary
    optimistic: true
    restore_mode: RESTORE_DEFAULT_OFF
    entity_category: config
    icon: mdi:gesture-tap-button

  - platform: template
    id: sw_button_3_click_momentary_enabled
    name: Button 3 click momentary
    optimistic: true
    restore_mode: RESTORE_DEFAULT_OFF
    entity_category: config
    icon: mdi:gesture-tap-button

number:
  - platform: template
    id: num_button_1_click_momentary_s
    name: Button 1 click momentary time
    optimistic: true
    restore_value: true
    entity_category: config
    icon: mdi:timer-outline
    unit_of_measurement: s
    min_value: 0.5
    max_value: 30
    step: 0.5
    mode: box
    initial_value: 1

  - platform: template
    id: num_button_2_click_momentary_s
    name: Button 2 click momentary time
    optimistic: true
    restore_value: true
    entity_category: config
    icon: mdi:timer-outline
    unit_of_measurement: s
    min_value: 0.5
    max_value: 30
    step: 0.5
    mode: box
    initial_value: 1

  - platform: template
    id: num_button_3_click_momentary_s
    name: Button 3 click momentary time
    optimistic: true
    restore_value: true
    entity_category: config
    icon: mdi:timer-outline
    unit_of_measurement: s
    min_value: 0.5
    max_value: 30
    step: 0.5
    mode: box
    initial_value: 1

  - platform: template
    id: num_button_1_long_press_momentary_s
    name: Button 1 long-press momentary time
    optimistic: true
    restore_value: true
    entity_category: config
    icon: mdi:timer-outline
    unit_of_measurement: s
    min_value: 0.5
    max_value: 30
    step: 0.5
    mode: box
    initial_value: 1

  - platform: template
    id: num_button_2_long_press_momentary_s
    name: Button 2 long-press momentary time
    optimistic: true
    restore_value: true
    entity_category: config
    icon: mdi:timer-outline
    unit_of_measurement: s
    min_value: 0.5
    max_value: 30
    step: 0.5
    mode: box
    initial_value: 1

  - platform: template
    id: num_button_3_long_press_momentary_s
    name: Button 3 long-press momentary time
    optimistic: true
    restore_value: true
    entity_category: config
    icon: mdi:timer-outline
    unit_of_measurement: s
    min_value: 0.5
    max_value: 30
    step: 0.5
    mode: box
    initial_value: 1

text:
  - platform: template
    id: txt_button_1_long_press_target
    name: Button 1 long-press target
    mode: text
    optimistic: true
    restore_value: true
    entity_category: config
    icon: mdi:form-textbox
    initial_value: ""
    min_length: 0
    max_length: 100

  - platform: template
    id: txt_button_2_long_press_target
    name: Button 2 long-press target
    mode: text
    optimistic: true
    restore_value: true
    entity_category: config
    icon: mdi:form-textbox
    initial_value: ""
    min_length: 0
    max_length: 100

  - platform: template
    id: txt_button_3_long_press_target
    name: Button 3 long-press target
    mode: text
    optimistic: true
    restore_value: true
    entity_category: config
    icon: mdi:form-textbox
    initial_value: ""
    min_length: 0
    max_length: 100

text_sensor:
  - platform: template
    id: ts_button_1_last_action
    name: Button 1 last action
    icon: mdi:gesture-tap
    entity_category: diagnostic

  - platform: template
    id: ts_button_2_last_action
    name: Button 2 last action
    icon: mdi:gesture-tap
    entity_category: diagnostic

  - platform: template
    id: ts_button_3_last_action
    name: Button 3 last action
    icon: mdi:gesture-tap
    entity_category: diagnostic

  - platform: template
    id: ts_last_button_event
    name: Last button event
    icon: mdi:timeline-text-outline
    entity_category: diagnostic
