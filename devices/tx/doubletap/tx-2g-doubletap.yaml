esphome:
  project:
    name: "AR Smart Home.TX-2GANG WITH DOUBLE TAP"
    version: "1.0.0"

esp8266:
  board: esp01_1m

logger:

binary_sensor:
  - platform: gpio
    pin:
      number: GPIO0
      mode:
        input: true
        pullup: true
      inverted: true
    id: button_1
    on_press:
      - script.execute:
          id: start_button_press
          button: 1
    on_release:
      - script.execute:
          id: release_button_press
          button: 1

  - platform: gpio
    pin:
      number: GPIO9
      mode:
        input: true
        pullup: true
      inverted: true
    id: button_2
    on_press:
      - script.execute:
          id: start_button_press
          button: 2
    on_release:
      - script.execute:
          id: release_button_press
          button: 2

  - platform: status
    name: "Device Status"

output:
  - platform: gpio
    pin: GPIO12
    id: relay_1

  - platform: gpio
    pin: GPIO5
    id: relay_2

light:
  - platform: binary
    name: "Switch 1"
    id: light_1
    output: relay_1
    restore_mode: RESTORE_DEFAULT_OFF

  - platform: binary
    name: "Switch 2"
    id: light_2
    output: relay_2
    restore_mode: RESTORE_DEFAULT_OFF

status_led:
  pin:
    number: GPIO13
    inverted: true

preferences:
  flash_write_interval: 1min
